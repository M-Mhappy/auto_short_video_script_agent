# 书籍解读演示重构方案

> 基于现有「智能口播稿生成器」架构，借鉴 Web Video Presentation 方法论，
> 砍掉冗余的 PPTX / Pillow / FFmpeg 视频合成链路，
> 改为「Web 演示 + 自动录屏 + 可选配音」的两路输出方案。

---

## 一、现状分析（问题定位）

### 现有链路

```
口播稿(## 分段) → LLM拆step JSON → PPTX导出 → Pillow渲染PNG → FFmpeg合成MP4
                                     ↘ Web播放器(前端 PresentationView)
```

### 核心问题

| 问题 | 具体表现 |
|------|----------|
| **视频质量差** | Pillow 渲染的图片配 FFmpeg 拼接 = 纯静态幻灯片感，文字边缘模糊，无动画无动效，不像视频 |
| **链路冗余** | `LLM步骤→PPTX→Pillow→FFmpeg` 四个中间环节，每步可能出错 |
| **PPTX 是多余中间格式** | PPTX 本身只为导出 PowerPoint，但用户主要需求是"出视频"，不是"出 PPT" |
| **Pillow 渲染能力天花板低** | 纯文字 + 纯色背景，做不到 CSS/SVG 级别的排版、渐变、动画 |
| **FFmpeg 合成** | 计算资源高、速度慢、画质受限于 PNG 分辨率 |
| **两套渲染逻辑** | PPTX/Pillow 用 slide_layout.py 渲染，Web 播放器用 React 渲染，维护双倍代码 |

### 根因

> 当前的「演示」本质是把**网页该做的事**交给了**离线图片渲染**。
> 网页本身就是最好的视频渲染引擎 —— 有 CSS 动画、SVG、Canvas、WebGL。
> 录屏就是最高性价比的"导出视频"方式。

---

## 二、重构目标

### 核心转变

```
旧：LLM拆step → PPTX → Pillow渲染 → FFmpeg合成视频
新：LLM拆step → React网页渲染 → [录屏 = 最终视频]
```

### 两路输出

```
                              ┌─ 有配音 → ?auto=1自动录屏 → 一镜到底成片
LLM拆step → React网页渲染 ──┤
                              └─ 无配音 → 手动录屏 + 详细配音稿(逐step标注)
```

### 具体指标

- ✅ 砍掉 PPTX / Pillow / FFmpeg / slide_layout.py 四个模块
- ✅ 视频质量从"静态幻灯片"提升到"CSS/SVG 动效演示"级别
- ✅ 录屏一镜到底，无需后期对音轨
- ✅ 配音稿逐 step 标注，方便用户自行配音（剪映/PR 对轨）
- ✅ 可复用现有 23 套视觉主题
- ✅ LLM 拆 step 升级为「内容驱动动画描述」，而非「纯文字 slide 类型」

---

## 三、重构后架构

### 3.1 模块变更总览

| 现有模块 | 操作 | 替代方案 |
|----------|------|----------|
| `backend/tools/presentation.py`（LLM拆step） | ⚠️ 重写 | 升级拆步格式，包含视觉演示描述 |
| `backend/tools/pptx_export.py` | ❌ 删除 | Web 渲染替代 |
| `backend/tools/slide_layout.py` | ❌ 删除 | CSS token 主题替代 |
| `backend/tools/video_export.py`（Pillow+FFmpeg） | ❌ 删除 | 录屏替代 |
| `backend/tools/tts.py` | ✅ 保留 | edge-tts 按步合成不变 |
| `frontend/src/presentation/` | ⚠️ 重写 | 升级为 16:9 固定舞台 + 主题系统 + auto 模式 |

### 3.2 新数据流

```
口播稿(##分段)
    │
    ▼
LLM 拆步（升级版）
    │  输出: 每step = { narration, screen: { type, text, visualHint } }
    │
    ▼
生成 React 章节代码（或运行时 JSON 驱动渲染）
    │
    ├── [用户选择: 有配音]
    │       │
    │       ▼
    │    edge-tts 按步合成 mp3
    │       │
    │       ▼
    │    Web 页面 `?auto=1`
    │       │ 按 Space → 自动播音频 → 音频结束自动推进下一步
    │       ▼
    │    打开 OBS / QuickTime 录屏
    │       │ 开始录制 → 按 Space → 等自动播完 → 停止录制
    │       ▼
    │    成品 MP4（音视频天然同步，无需后期）
    │
    └── [用户选择: 无配音]
            │
            ▼
        生成「配音稿.md」
            │ 逐 step 标注: "Step 3: 屏幕显示xxx，口播: '...'"
            ▼
        手动录屏（点击推进）+ 后期自行配音

    两者均可【预览】Web 演示（不在录屏阶段也能点着玩）
```

### 3.3 API 变更

| 端点 | 操作 | 说明 |
|------|------|------|
| `POST /presentation/generate` | ⚠️ 重写 | 输出从 PPTX 格式改为 Web 渲染数据 |
| `POST /presentation/tts` | ✅ 保留（微调） | 按步 edge-tts → `public/audio/<chapter>/<N>.mp3` |
| `POST /presentation/video` | ❌ 删除 | 不再需要服务端合成视频 |
| `GET /presentation/download-video` | ❌ 删除 | 视频通过录屏产生，不走服务端 |
| `POST /presentation/narration-script` | ✨ 新增 | 生成逐 step 配音稿 .md 下载 |
| `GET /presentation/download-pptx` | ❌ 删除 | 不再支持 PPTX |
| `GET /presentation/download-zip`->web | ✨ 新增 | 下载完整 Web 演示项目压缩包（可选，高级用户） |

---

## 四、核心升级点

### 4.1 LLM 拆步格式升级（最关键）

**旧格式**（只描述文字布局类型）：

```json
{
  "type": "hero",
  "text": "认知失调：当行为与信念冲突"
}
```

**新格式**（包含视觉演示描述，给 React 渲染引擎用）：

```json
{
  "step": 1,
  "narration": "费斯廷格提出了一个让人不安的理论——认知失调。",
  "screen": {
    "headline": "认知失调",
    "subhead": "当行为与信念冲突",
    "visual": {
      "type": "reveal",           // reveal | split | grow | flow | timeline | quote | compare
      "elements": [
        {
          "kind": "text",
          "content": "信念",
          "role": "left-label"
        },
        {
          "kind": "text",
          "content": "行为",
          "role": "right-label"
        },
        {
          "kind": "arrow",
          "from": "信念",
          "to": "行为",
          "label": "不一致 → 不舒服",
          "animate": "draw"       // 箭头自绘动画
        }
      ],
      "mood": "tense"             // 提示动画气质：tense / calm / dramatic / playful
    }
  }
}
```

关键变化：
- **`visual.type`** 代替旧 `type` —— 表示「视觉演示动作」，不是「文字布局模板」
- **`visual.elements`** 描述屏幕上有什么元素、它们之间什么关系
- **`visual.mood`** 给渲染引擎提示动画节奏气质
- **`narration`** 是该步的口播文本（与配音稿共用）

**LLM Prompt 升级要点**：给 LLM 的拆步指令从"把这段文字分配到 hero/text/quote/list_item 模板"改为：

> **将每段口播拆分为视觉步。每步聚焦一个想法。**
> 屏幕在 **演** 这件事，不是在贴文字。
> 描述屏幕上有什么视觉元素、它们之间什么空间/逻辑关系、用什么动作揭示。
>
> visual.type 可选：
> - `reveal` — 核心文字/画面从模糊到清晰/从暗到亮
> - `split` — 屏幕一分为二，展示对比
> - `grow` — 数字/图表从零增长到目标值
> - `flow` — 流程节点依次点亮，连线自绘
> - `timeline` — 时间轴上事件逐个展开
> - `quote` — 金句引用，带引号 + 出处 + 淡入
> - `compare` — 左右/上下对比
> - `list` — 逐项揭示（1 项 = 1 step，这里只描述当前项）
> - `scene` — 场景/氛围画面（书籍时代背景、人物场景等）
>
> 禁止：纯文字居中显示。每步至少有一个视觉动作。

### 4.2 前端渲染引擎升级

**旧**：`frontend/src/presentation/` 中简单的 stepper + 文字渲染

**新**：采用 16:9 固定舞台架构

```
frontend/src/presentation/
├── Stage.tsx                    # 16:9 固定舞台(1920×1080) + transform scale
├── hooks/
│   ├── useStepper.ts            # 全局 step 计数器（无定时器）
│   ├── useAudioPlayer.ts        # 音频播放 + auto 模式自动推进
│   └── useAutoMode.ts           # mode 切换: manual / audio / auto
├── components/
│   ├── AutoStartGate.tsx        # "Press SPACE to start" 蒙层
│   ├── ProgressBar.tsx          # 悬浮显示进度条
│   └── AutoToggle.tsx           # 隐藏的模式切换按钮
├── styles/
│   ├── tokens.css               # 主题色板 + 字体 token（按书可选主题）
│   ├── base.css                 # 舞台布局、进度条、过渡
│   └── animations.css           # 通用动效 keyframes
├── themes/                      # 预置 6-8 套适合书籍解读的主题
│   ├── warm-paper/              # 暖纸墨色 — 文学/历史
│   ├── midnight-press/          # 深沉电影感 — 悬疑/社科
│   ├── chalk-garden/            # 手写粉笔感 — 教育/科普
│   ├── newsroom/                # 报刊严谨风 — 非虚构/传记
│   ├── forest-ink/              # 森林绿墨 — 自然/哲学
│   └── kraft-paper/             # 牛皮纸 — 怀旧/文学
└── chapters/                    # 每步一个渲染组件（由 LLM 拆步数据动态渲染）
    └── renderStep.ts            # 根据 LLM step.screen.visual.type 分发到不同渲染器
```

#### 渲染策略（两种方案二选一）

**方案 A：运行时 JSON 驱动（推荐，改动最小）**

LLM 拆步输出 JSON → 前端一个通用 `renderStep.ts` 按 `visual.type` 分发到不同渲染组件（`RevealRenderer` / `SplitRenderer` / `QuoteRenderer` / `ListRenderer` / ...）

```tsx
// renderStep.ts 核心逻辑
const RENDERERS: Record<string, StepRenderer> = {
  reveal: RevealRenderer,
  split: SplitRenderer,
  grow: GrowRenderer,
  flow: FlowRenderer,
  timeline: TimelineRenderer,
  quote: QuoteRenderer,
  compare: CompareRenderer,
  list: ListItemRenderer,
};

function renderStep(step: StepData, active: boolean) {
  const Renderer = RENDERERS[step.screen.visual.type] || TextRenderer;
  return <Renderer data={step} active={active} />;
}
```

**方案 B：代码生成（效果更惊艳，但工程量大）**

LLM 拆步时直接输出 React 组件代码 + CSS，保存到 `chapters/` 目录。适合需要高度定制视觉的书籍。**V2 再考虑，MVP 走方案 A。**

### 4.3 主题系统

从 web-video-presentation 的 23 套主题中精选 6-8 套适合「书籍解读」场景的，预置 token。

每套主题 = `tokens.css`（颜色 + 字体 + 性格旋钮）：

```css
/* warm-paper 主题节选 */
:root {
  --shell: #e8dcc8;            /* letterbox 背景 */
  --surface: #f5efe0;          /* 舞台主背景 - 暖纸 */
  --text: #2c2420;             /* 主文字 - 墨色 */
  --text-2: #5a4a42;           /* 次文字 */
  --accent: #c0392b;           /* 品牌强色 - 朱红 */
  --accent-soft: rgba(192,57,43,0.12);
  --font-display-cn: "Noto Serif SC", serif;
  --font-body: "Source Han Serif CN", serif;
  --font-mono: "JetBrains Mono", monospace;
  --r-card: 4px;
  --rule-w: 1px;
  --rule-style: solid;
  --hero-num-font: var(--font-display-cn);
}
```

切换主题 = 替换 `tokens.css`，渲染代码一行不动。

### 4.4 配音稿生成（无配音路径）

新增 `POST /presentation/narration-script` 端点，生成 Markdown 文件：

```markdown
# 《乌合之众》配音稿

> 总步数：24 步 | 预估时长：~8 分钟
> 使用方式：打开演示网页 → 开始录屏 → 按此稿逐句配音

---

## 章节 1：群体心理的特征

### Step 1（~12 秒）
**屏幕**：标题「群体心理」以大号衬线字从暗到亮淡入，背景是模糊的人群剪影
**口播**：「古斯塔夫·勒庞在 1895 年写下了一个让所有统治者都不安的观点——当人们聚集成群体，智商会集体下降。」

### Step 2（~8 秒）
**屏幕**：「无意识」「本能」「易传染」三个关键词从左侧逐个 fly-in
**口播**：「群体的三个核心特征：无意识、本能驱动、极易传染。」

### Step 3（~10 秒）
**屏幕**：左侧显示「独立思考的个体」，右侧渐变为「盲从的群体」，中间有断裂动画
**口播**：「一个人时他可能是理性的绅士。一旦进入群体——你猜怎么着——他变成了跟着口号走的狂热分子。」

---
```

**格式约定**：
- `### Step N（~X 秒）` — step 序号 + 口播估时
- `**屏幕**` — 该步视觉内容的文字描述（方便用户理解当前画面）
- `**口播**` — 该步要念的完整文本
- **屏幕描述不用极详细**，告诉用户"这步画面上是什么"就够了

---

## 五、录屏 vs 服务端合成视频对比

| 维度 | 录屏（推荐） | 服务端合成 FFmpeg（❌ 删除） |
|------|-------------|---------------------------|
| **画质** | 原生浏览器渲染，CSS/SVG/Canvas 无损 | Pillow 渲染 PNG，文字边缘锯齿 |
| **动效** | CSS 动画、SVG 路径自绘、粒子系统 | 无，纯静态切换 |
| **开发成本** | 前端写好就行，录屏是标准操作 | 需维护渲染 + 编码 + 同步三套逻辑 |
| **部署成本** | 零。录屏在用户本地完成 | 服务端需装 FFmpeg，CPU 密集 |
| **后期灵活性** | 录完就是 MP4，可剪裁头尾 | 同样需要后期剪裁 |
| **用户门槛** | 需安装 OBS / QuickTime | 无需，但输出质量差 |
| **性价比** | **高** — 浏览器渲染 0 成本，录屏免费 | **低** — 开发维护成本高、质量低 |

**结论**：录屏在画质、性能、开发成本三个维度全面优于服务端合成。FFmpeg 方案唯一优势是"服务端一键生成"，但牺牲了所有视频质量。**录屏是唯一的推荐路径。**

---

## 六、分步实施计划

### Phase 1：砍掉冗余模块（1-2 天）

```bash
# 删除
backend/tools/pptx_export.py          # PPTX 导出
backend/tools/slide_layout.py          # 排版逻辑
backend/tools/video_export.py          # Pillow + FFmpeg 视频合成
```

同时：
- `POST /presentation/video` 端点标记 deprecated → 删除
- `GET /presentation/download-video` 删除
- `GET /presentation/download-pptx` 删除
- 更新 `技术文档.md` 模块表格

### Phase 2：升级 LLM 拆步指令（1 天）

修改 `backend/tools/presentation.py` 中调 LLM 的 prompt：

**输入不变**：口播稿（`## ` 分段）

**输出格式改变**：从旧 slide 格式 → 新 visual 格式（含 `visual.type` + `elements` + `mood`）

**需要做**：
1. 更新 `backend/prompts/templates.py` 中的拆步 prompt
2. 加一个输出格式校验函数（确保 LLM 输出合法 JSON）
3. 更新 `backend/tools/chapters.py` 解析逻辑（如需要）

### Phase 3：重写前端演示引擎（3-5 天）

核心任务：
1. **16:9 固定舞台** — `Stage.tsx` + `useStepper.ts` + `useAudioPlayer.ts`
2. **主题系统** — 精选 6-8 套主题，`tokens.css` 按书可选
3. **渲染器组件** — 按 `visual.type` 分发的渲染器（`RevealRenderer` 等）
4. **Auto 模式** — `AutoStartGate` + 音频推进逻辑
5. **配音稿生成** — 新增端点 + 渲染模板

**建议并行**：主题系统 + 渲染器组件可以同步开发，不互相依赖。

**测试方法**：Mock 一份 LLM 拆步 JSON 数据，在前端逐步渲染校验。

### Phase 4：新增配音稿端点（0.5 天）

```python
# backend/routers/presentation.py 新增

@router.post("/presentation/narration-script")
async def generate_narration_script(session_id: str):
    """生成逐 step 配音稿 Markdown，供用户下载自行配音"""
    session = load_session(session_id)
    steps = session.presentation_steps
    md = render_narration_script(steps)  # 渲染为上述格式
    return StreamingResponse(io.BytesIO(md.encode()), media_type="text/markdown",
        headers={"Content-Disposition": f"attachment; filename=配音稿.md"})
```

### Phase 5：清理前端旧代码（0.5 天）

- 删除旧 `frontend/src/presentation/` 中的 PPTX 预览相关代码
- 删除 `slide_layout.ts` 在前端的调用（如有）
- 更新前端 API 调用

---

## 七、两路流程用户交互

### 路径 A：合成配音 + 自动录屏（推荐）

```
用户确认口播稿 → 点击「生成演示」
    → LLM 拆步 → 预览演示效果
    → 点击「合成配音」
        → edge-tts 按步合成
        → 提示：打开录屏工具（OBS/QuickTime），按 SPACE 开始
        → 用户录屏 → 停止 → 裁头尾 → 成品
```

### 路径 B：无配音 + 自行配音

```
用户确认口播稿 → 点击「生成演示」
    → LLM 拆步 → 预览演示效果
    → 点击「下载配音稿」
        → 下载 `配音稿.md`
    → 用户打开演示网页，开始录屏（手动点击推进）
    → 用户用剪映/PR，按配音稿逐句配音
```

### 用户首次使用引导

在演示预览页加一个「录屏指引」按钮，弹出：

```
🎬 如何导出视频

方案一：自动配音（推荐）
  1. 点击「合成配音」
  2. 打开 OBS / QuickTime
  3. 浏览器全屏，打开 ?auto=1
  4. 开始录屏 → 按 SPACE → 自动播完 → 停止
  5. 裁掉头尾 → 发布 ✅

方案二：自行配音
  1. 点击「下载配音稿」
  2. 打开录屏，手动点击推进
  3. 后期按配音稿配音
```

---

## 八、关键设计决策记录

| 决策 | 理由 |
|------|------|
| **砍 PPTX** | 用户要的是"视频"不是 PPT。PPTX 导出是中间产物思维，应移除 |
| **砍 Pillow+FFmpeg** | 视频质量低、维护成本高。浏览器本身就是最好的渲染引擎 |
| **改用 16:9 CSS stage** | 1920×1080 固定画布 + `transform: scale()` = 清晰度无损 + 纯 CSS 无需 Canvas |
| **LLM 拆步含 visual.type** | 让 LLM 参与视觉设计决策（内容驱动动画），而非纯文字布局 |
| **主题 token 系统** | 一本书一套视觉气质。颜色/字体走 token，一步切换，无需改代码 |
| **配音稿独立输出** | 满足"想自己配音"的用户需求，同时可做"AI 配音前预览" |
| **录屏为最终输出** | 录屏 = 浏览器渲染 + 屏幕捕获，画质最高、成本最低、无需服务端计算 |

---

## 九、风险与缓解

| 风险 | 缓解方案 |
|------|----------|
| LLM 拆步 JSON 格式不稳定 | 加 Pydantic 校验 + 格式修复 Prompt + 降级到纯文字渲染 |
| 某些书籍不适合视觉演示 | 渲染器兜底：纯文字 + 大字体 + 留白 + 一本书的色调用主题 token 撑 |
| 用户不会录屏 | 录屏指引 + 图文步骤（截图 OBS 怎么设置） |
| auto 模式音画不同步 | AudioPlayer 以 `ended` 事件为推进信号，误差 < 50ms，无需担心 |
| 前端渲染引擎开发量大 | Phase 1 砍掉后端冗余模块腾出时间；渲染器按 visual.type 逐步实现，先做 reveal / list / quote 三个最常用类型 |

---

## 附录 A：LLM 拆步 Prompt（新版）

```python
PRESENTATION_STEP_PROMPT = """
你是一位书籍解说的视觉导演。将一段口播稿拆分为视觉演示步骤。

## 输入格式

口播稿以 `## 章节标题` 分段。你需要将**每段**口播拆成若干步骤。

## 每个步骤的输出格式

```json
{
  "step": 1,
  "narration": "该步口播文本（来自口播稿原文或微调断句）",
  "screen": {
    "headline": "核心标题（如有，显示在画面主位）",
    "subhead": "副标题/补充信息（可选）",
    "visual": {
      "type": "reveal | split | grow | flow | timeline | quote | compare | list | scene",
      "elements": [
        {
          "kind": "text | number | image | quote | arrow | divider | icon",
          "content": "元素内容",
          "role": "描述该元素在画面中的功能角色",
          "animate": "fade-in | fly-in | draw | grow | typewriter | none"
        }
      ],
      "mood": "calm | tense | dramatic | playful | warm | mysterious"
    }
  }
}
```

## 视觉类型说明

| type | 何时用 | 例子 |
|------|--------|------|
| reveal | 展示核心观点/标题/金句 | 先黑屏，核心文字从暗到亮淡入 |
| split | 展示对比/矛盾 | 左边旧观点，右边新观点，中间一道裂缝 |
| grow | 展示数字/数据增长 | "10万册"数字从 0 涨到目标值 |
| flow | 展示流程/时间线 | 三个节点依次点亮，连线自绘 |
| timeline | 历史脉络/事件顺序 | 时间轴上事件卡片逐个展开 |
| quote | 引用书中金句 | 大引号 + 金句文字 + 出处 |
| compare | 左右对照 | 左: 勒庞的观点, 右: 现代研究的修正 |
| list | 逐项揭示（1项=1步） | 三个特征逐个亮起，前面的灰化 |
| scene | 还原书籍场景/时代氛围 | 背景氛围画面 + 年份/地点角标 |

## 约束

- **每步聚焦一个想法**。口播一句切换一个想法时，必须切 step。
- **1 项 = 1 step**。清单必须逐个揭示，禁止一次性全显示。
- **双源原则**：节奏跟口播稿走，但画面可以补充书中具体年份/人名/数据（LLM 参数记忆中提取）。
- **禁止纯文字居中**。每步至少一个视觉动作（淡入/飞入/增长/绘制等）。
- **禁止编造**。书中没有的数据/案例不要出现。
- **画面信息密度 > 口播信息密度**。屏幕上可挂书中具体的年份、地名、原话片段等细节。

## 参考示例

口播段落：
"勒庞提出了一个核心观点：群体是盲目冲动的。一个人时他可能很理性，但一旦融入群体，智商就会直线下降。"

对应输出（示意，不要照抄结构，根据内容设计视觉动作）：
```json
{
  "step": 1,
  "narration": "勒庞提出了一个核心观点：群体是盲目冲动的。",
  "screen": {
    "headline": "群体 = 盲目 × 冲动",
    "visual": {
      "type": "split",
      "elements": [
        {"kind": "text", "content": "个体理性", "role": "左侧标签", "animate": "fade-in"},
        {"kind": "text", "content": "群体盲从", "role": "右侧标签", "animate": "fade-in"},
        {"kind": "arrow", "content": "↓", "role": "从理性滑向盲从的箭头", "animate": "draw"}
      ],
      "mood": "tense"
    }
  }
}
```

直接输出 JSON 数组，无其他文字。
"""
```

## 附录 B：录屏指引（产品内嵌图文）

在演示预览页增加「导出视频」按钮，展示：

```
┌─────────────────────────────────────────┐
│  🎬 导出视频                            │
│                                         │
│  方案一：AI 配音自动录屏（推荐）         │
│  ┌─────────────────────────────────┐   │
│  │ ① 点击「合成配音」              │   │
│  │ ② 打开 OBS Studio               │   │
│  │ ③ 添加来源 → 窗口捕获 → 选浏览器 │   │
│  │ ④ 浏览器全屏 F11                │   │
│  │ ⑤ 在 URL 末尾加 ?auto=1         │   │
│  │ ⑥ OBS 开始录制                  │   │
│  │ ⑦ 按 SPACE → 自动播完 → 停止录制 │   │
│  │ ⑧ 裁掉头尾 → 导出 ✅             │   │
│  └─────────────────────────────────┘   │
│                                         │
│  方案二：自行配音                       │
│  ┌─────────────────────────────────┐   │
│  │ ① 点击「下载配音稿」            │   │
│  │ ② OBS 录屏（手动点击推进）      │   │
│  │ ③ 剪映/PR 按配音稿配音          │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

---

*本方案基于现有「智能口播稿生成器」架构与「Web Video Presentation」方法论编写。*
*核心思想：网页是最好的视频引擎，录屏是最好的导出方式。*
