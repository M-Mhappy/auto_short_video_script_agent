# 智能口播稿生成器 — 技术文档

## 一、架构总览

- 前端：React + Vite + TailwindCSS，对话式 UI
- 后端：FastAPI + LangGraph 状态机，SSE 实时推送
- LLM：DeepSeek（通过 OpenAI 兼容接口）
- 搜索：Tavily Search API
- 持久化：SQLite（LangGraph AsyncSqliteSaver）
- 导出：python-docx

## 二、核心模块

| 模块 | 路径 | 职责 |
|------|------|------|
| 状态定义 | `backend/graph/state.py` | AgentState 字段，驱动全流程数据流转 |
| 状态机构建 | `backend/graph/builder.py` | 12 个节点 + 5 条条件路由，编排完整工作流 |
| 入口路由 | `backend/graph/nodes/entry.py` | LLM 判断用户输入属于关键词模式还是直接指定模式 |
| 书籍搜索 | `backend/graph/nodes/book_search.py` | Tavily 搜索 + LLM 格式化去重，支持重试去重 |
| 书籍确认 | `backend/graph/nodes/book_confirm.py` | HP-1 中断点，支持选择/重试/换词搜索/放弃 |
| 口播稿生成 | `backend/graph/nodes/generate_script.py` | 核心生成节点，末尾自动追加固定署名 |
| 审核打分 | `backend/graph/nodes/review_script.py` | 四维度评分 + 联网交叉验证 |
| 重试控制 | `backend/graph/nodes/retry_generate.py` | 60-80 分自动重试，最多 2 次 |
| 低质量决策 | `backend/graph/nodes/low_quality.py` | HP-2 中断点，<60 分交用户决策 |
| 预览反馈 | `backend/graph/nodes/preview_script.py` | HP-3 中断点，确认/反馈/放弃 |
| 反馈应用 | `backend/graph/nodes/apply_feedback.py` | 根据用户反馈精准修改，重新追加署名 |
| Word 导出 | `backend/graph/nodes/export_word.py` | python-docx 通用格式导出 |
| API 层 | `backend/main.py` | FastAPI 路由 + SSE 推送 + 中断恢复 |
| 搜索工具 | `backend/tools/search.py` | Tavily 封装，支持书籍信息搜索和审核搜索 |
| Prompt 模板 | `backend/prompts/templates.py` | 6 个核心 Prompt |
| 前端入口 | `frontend/src/App.tsx` | 会话管理 + SSE 监听 + 中断交互编排 |

## 三、数据流

```
用户输入 → entry(路由) → [keyword提取] → book_search(Tavily)
→ confirm_book(HP-1中断) → load_reference → generate_script
→ review_script → [>80通过 / 60-80重试 / <60低质量决策]
→ preview_script(HP-3中断) → [确认→export_word / 反馈→apply_feedback循环]
```

## 四、前后端通信

- REST API 触发操作（创建会话、启动、中断恢复）
- SSE 实时推送节点执行事件到前端
- 中断点通过 LangGraph `interrupt()` 暂停，前端展示交互 UI，用户操作后通过 `Command(resume=...)` 恢复

## 五、问题与解决记录

| 日期 | 问题 | 解决措施 |
|------|------|----------|
| 2026-05-22 | `AsyncSqliteSaver.from_conn_string` 返回异步上下文管理器，非直接实例 | lifespan 中改用 `async with` 包裹 |
| 2026-05-22 | Python 3.14 与 Pydantic V1 兼容警告 | 不影响功能，LangChain 内部依赖，暂忽略 |
| 2026-05-22 | 豆瓣 API 不可用，原设计数据源方案不可行 | MVP 改用 Tavily Search API 统一搜索 |
