# 短视频口播稿生成 Agent — Cursor 开发规格书

> 面向个人创作者的口播稿生成 Agent。围绕一本书生成 8-15 分钟讲故事式口播稿。
> 范式：LangGraph 状态机 + Workflow + Tool-using + Human-in-the-loop。

---

## 一、核心工作流

### 两种入口

| 模式 | 流程 |
|------|------|
| **模式一：关键词探索** | 用户输入关键词/描述 → LLM 提取关键词 → 联网检索书籍 → 2-5 个候选项（书名+作者+简介）→ 用户选择 |
| **模式二：直接指定** | 用户直接输入书名（+作者）→ 联网检索确认 → 返回确认项 → 用户确认 |

### 统一后续流程

```
书籍信息确认 → 口播稿生成 → 审核打分 → 预览/反馈 → Word 输出 → [可选] AI 配音
                ↑                    ↓
                └──── 用户修改反馈（无上限循环）
```

1. **书籍信息确认**：展示书名+作者+简介，用户确认或更换。
2. **口播稿生成**：
   - 输入：书籍信息 + 参考示例（`reference_script.md`，预留接口，用户后续上传）
   - LLM 分析参考示例提取句式结构（仅借鉴结构层面）
   - 风格由 LLM 根据书籍主旨自动判定（幽默、严肃、积极等），不固定
   - 时代背景：系统自动弱关联当前社会现实
   - 要求：8-15 分钟、第三人称、讲故事式转述、**概述整本书全部章节**
   - 格式：纯文本，LLM 自行把握叙事结构，无分镜/朗读提示
3. **审核打分（默认轻量路线）**：
   - LLM 审核员基于自身参数记忆 + 联网搜索该书核心情节/论点，与转述稿交叉验证
   - 输出 JSON 四维度评分（各 0-25，满分 100）：
     - 事实准确性：有无编造原文不存在的情节/论点
     - 忠实度：转述是否歪曲作者原意
     - 完整性：是否遗漏全书核心章节关键内容
     - 风格一致性：是否符合该书主旨应有的风格
   - 规则：>80 通过；60-80 自动重试 1-2 次；**<60 提示用户"该书信息不足，建议换一本热门书"或接受当前稿件**
4. **用户反馈循环**：预览 → 确认 / 提交修改建议 → 输出 Word（通用默认格式）
   - 反馈修改无上限，直至用户确认

---

## 二、架构概述

### 模块

- **Entry Router**：区分模式一/模式二
- **Keyword Extractor**（模式一）：从描述提取检索关键词
- **Book Search Engine**：多源检索（豆瓣 API + 百科 + 微信读书简介，Z-Library 补充）
- **Book Confirmation**（中断点）：展示书籍信息，等用户确认
- **Script Generator**：口播稿生成核心
- **Script Reviewer**：四维度审核打分
- **Retry Controller**：60-80 分自动重试（最多 2 次）
- **Feedback Processor**：用户修改意见循环应用
- **Word Exporter**：python-docx 通用格式导出
- **TTS Engine**：edge-tts 文字转语音，生成 MP3 音频（可选，独立于主流程）
- **Orchestrator**：LangGraph 状态机驱动

### 数据流

```
用户输入 → 入口路由 → (关键词提取 → ) 书籍检索 → [HP-1: 书籍确认]
→ 加载参考示例 → 生成口播稿 → 审核打分
  ├─ >80 → [HP-3: 预览反馈] → Word 输出 → [可选] AI 配音(edge-tts)
  ├─ 60-80 → 重试生成 → 重新审核
  └─ <60 → [HP-2: 低质量决策] → 接受当前稿 / 放弃换书
```

### 三个中断点（Human-in-the-loop）

| 中断点 | 内容 | 用户操作 |
|--------|------|----------|
| HP-1 书籍确认 | 书名/作者/简介 | 确认 / 重新检索 / 放弃 |
| HP-2 低质量决策 | 审核分数+问题说明 | 接受当前稿 / 放弃换书 |
| HP-3 预览反馈 | 口播稿全文 | 确认输出 / 提交修改建议 |

---

## 三、LangGraph 状态机（节点与流转）

节点列表：`entry` → `extract_keywords`(模式一) / `search_books`(模式二) → `confirm_book`(中断) → `load_reference` → `generate_script` → `review_script` → (`retry_generate`) → `low_quality_decision`(中断) → `preview_script` → `collect_feedback`(中断) → `apply_feedback` → `export_word` → `end`

流转规则：
- `review_script`: >80 → preview；60-80 且重试<2 → retry_generate → generate_script；否则 → low_quality_decision
- `low_quality_decision`: 接受 → preview_script；放弃 → end
- `collect_feedback`: 确认 → export_word；修改 → apply_feedback → preview_script

状态持久化到本地 SQLite，支持中断恢复。

---

## 四、Prompt 模板（核心约束）

### 4.1 关键词提取（模式一）

```
你是一位书籍检索专家。从用户描述中提取 3-8 个检索关键词。

用户输入：{user_input}

要求：
- 覆盖主题领域、情绪基调、目标受众、书籍类型
- 优先中文，必要时补充英文
- 仅返回 JSON 数组，无解释

示例：["心理学", "人际关系", "非暴力沟通", "职场"]
```

### 4.2 书籍检索格式化

```
你是一位书籍信息整理专家。将多源原始检索数据整理为标准格式。

原始数据：{raw_search_results}

规则：
- 去重（ISBN/书名+作者相同则合并，优先豆瓣数据）
- 补全缺失简介
- 剔除明显不相关条目
- 按相关性降序保留 2-5 项

输出 JSON 数组：
[{"title": "", "author": "", "isbn": "", "intro": "100-200字", "source": "", "relevance_reason": ""}]
```

### 4.3 口播稿生成

```
你是一位资深短视频口播稿撰稿人，擅长将书籍内容转化为讲故事式口播稿。

书籍信息：
书名：{book_title}
作者：{book_author}
简介：{book_intro}

参考示例（仅借鉴句式结构，如存在）：
{reference_script}

任务：撰写 8-15 分钟口播稿（约 2000-3500 字）。

核心要求：
1. 第三人称叙述
2. 讲故事式转述，有起承转合
3. 概述整本书全部章节核心内容，不局限于片段
4. 风格由你根据书籍主旨自动判定（幽默/严肃/积极/治愈/批判等）
5. 弱关联当前社会现实，自然融入，不生硬说教
6. 纯文本，自行把握叙事结构，无特殊排版标记
7. 禁止编造原文不存在的情节、论点、案例

叙事建议（灵活调整）：
- 开场钩子 → 背景铺垫 → 分章转述 → 高潮转折 → 现实映照 → 收尾升华

直接输出口播稿纯文本。
```

### 4.4 审核打分

```
你是一位严格的书籍内容审核员。审核 AI 生成的口播稿是否忠实、准确地转述了原书。

审核方法：
1. 基于参数记忆回顾该书核心情节、论点、章节结构
2. 结合联网搜索补充验证（如可用）

书籍：{book_title} / {book_author}
待审核稿：{script_draft}
联网补充：{web_search_results}

评分维度（各 0-25，满分 100）：
1. 事实准确性：有无编造原文不存在的情节/论点/案例/数据
2. 忠实度：转述是否歪曲作者原意，有无断章取义
3. 完整性：是否遗漏全书核心章节关键内容
4. 风格一致性：风格是否符合该书主旨应有的基调

输出严格 JSON，无其他文字：
{
  "fact_accuracy": 0-25,
  "fidelity": 0-25,
  "completeness": 0-25,
  "style_consistency": 0-25,
  "total": 0-100,
  "reasoning": "总体评价与详细理由，200-400字",
  "hallucinations": ["编造点1"],
  "omissions": ["遗漏点1"]
}

规则：
- 如无问题可给满分
- hallucinations/omissions 无则填 []
- 总分 = 四维之和
- 理由必须具体，指出问题所在段落
```

### 4.5 用户反馈应用

```
你是一位口播稿修改专家。根据用户反馈精准修改，仅改用户提到的问题，不擅自大改。

当前稿件：{current_script}
用户反馈：{user_feedback}

要求：
- 保持整体叙事结构和风格一致
- 反馈模糊时增加细节描写或情绪渲染
- 要求增加某章节时，在不编造前提下补充
- 要求删减时优先删重复或次要内容

输出修改后的完整稿件全文（不要只输出片段）。
```

---

## 五、审核评分执行规则

- 通过：> 80 分，直接输出预览
- 重试：60-80 分，自动重试最多 2 次，仍不过交用户决策
- 不达标：< 60 分，**不重试**，提示"该书信息不足，建议换一本热门书"或接受当前稿件
- 审核模型：deepseek v4-pro

---

## 六、关键约束（必须遵守）

| # | 约束 |
|---|------|
| 1 | 仅 LLM 参数记忆 + 联网搜索，无 RAG |
| 2 | < 60 分不重试，提示换书或接受当前稿 |
| 3 | 反馈修改无上限 |
| 4 | 口播稿必须概述整本书全部章节 |
| 5 | 热点搜集、图片生成已砍掉 |
| 6 | Word 通用默认格式，无特殊排版 |
| 7 | 时代背景弱关联，系统自动处理 |
| 8 | 参考示例文件命名为 `reference_script.md`，当前预留接口 |
| 9 | 人在回路：书籍确认、低质量决策、预览反馈必须人工 |

---





